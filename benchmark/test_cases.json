[
  {
    "id": "Q2",
    "difficulty_weight": 1.5,
    "function": "archive_free",
    "question": "What is the exact signature of `archive_free` (return type and parameters)? List each resource it releases in source order, naming the exact deallocation function used for each.",
    "rubric": [
      {
        "criterion": "Signature is `void archive_free(ArchiveApp* archive)`",
        "points": 1
      },
      {
        "criterion": "Removes all three views from view_dispatcher before calling `view_dispatcher_free`",
        "points": 1
      },
      {
        "criterion": "Closes furi records in reverse-open order: `RECORD_LOADER` then `RECORD_GUI`",
        "points": 1
      },
      {
        "criterion": "`free(archive)` is the final deallocation call",
        "points": 1
      },
      {
        "criterion": "Mentions `furi_string_free(archive->fav_move_str)` and `text_input_free(archive->text_input)`",
        "points": 1
      }
    ]
  },
  {
    "id": "Q13",
    "difficulty_weight": 1.0,
    "function": "infrared_tx_stop",
    "question": "What is the exact signature of `infrared_tx_stop` (return type and parameters)? What guard condition causes it to return early without doing anything? List every action it performs in source order when it does not return early.",
    "rubric": [
      {
        "criterion": "Signature is `void infrared_tx_stop(InfraredApp* infrared)`",
        "points": 1
      },
      {
        "criterion": "Returns early when `infrared->app_state.is_transmitting` is false",
        "points": 1
      },
      {
        "criterion": "Calls `infrared_worker_tx_stop(infrared->worker)` first",
        "points": 1
      },
      {
        "criterion": "Calls `infrared_worker_tx_set_get_signal_callback(infrared->worker, NULL, NULL)` next",
        "points": 1
      },
      {
        "criterion": "Calls `infrared_play_notification_message` with `InfraredNotificationMessageBlinkStop`, then sets `is_transmitting = false` and records `last_transmit_time = furi_get_tick()`",
        "points": 1
      }
    ]
  },
  {
    "id": "Q16",
    "difficulty_weight": 1.0,
    "function": "ArchiveApp (search)",
    "question": "What are all the fields of the `ArchiveApp` struct? List each field's type and name in the exact order they appear in the struct definition.",
    "rubric": [
      {
        "criterion": "First four fields in order: `Gui* gui`, `Loader* loader`, `ViewDispatcher* view_dispatcher`, `SceneManager* scene_manager`",
        "points": 2
      },
      {
        "criterion": "Middle fields in order: `ArchiveBrowserView* browser`, `TextInput* text_input`, `Widget* widget`, `FuriPubSubSubscription* loader_stop_subscription`",
        "points": 2
      },
      {
        "criterion": "Remaining fields include `FuriString* fav_move_str`, `char text_store[MAX_NAME_LEN]`, and `char file_extension[MAX_EXT_LEN + 1]` in that order",
        "points": 1
      }
    ]
  },
  {
    "id": "Q17",
    "difficulty_weight": 1.5,
    "function": "gpio_items.c (find_functions_in_file)",
    "question": "List all functions defined in `gpio_items.c`. Give only function names.",
    "rubric": [
      {
        "criterion": "Total count is exactly 8 functions",
        "points": 1
      },
      {
        "criterion": "Lists all 8 functions: `gpio_items_alloc`, `gpio_items_free`, `gpio_items_get_count`, `gpio_items_configure_pin`, `gpio_items_configure_all_pins`, `gpio_items_set_pin`, `gpio_items_set_all_pins`, `gpio_items_get_pin_name`",
        "points": 2
      }
    ]
  },
  {
    "id": "Q18",
    "difficulty_weight": 2.0,
    "function": "scene_manager_handle_back_event (traverse)",
    "question": "Which functions in the firmware directly call `scene_manager_handle_back_event`? List all of them by name.",
    "rubric": [
      {
        "criterion": "Lists at least 7 distinct caller functions",
        "points": 1
      },
      {
        "criterion": "Includes `archive_back_event_callback` and `gpio_app_back_event_callback`",
        "points": 1
      },
      {
        "criterion": "Includes `nfc_back_event_callback` and `infrared_back_event_callback`",
        "points": 1
      },
      {
        "criterion": "Includes `subghz_back_event_callback` and `u2f_app_back_event_callback`",
        "points": 1
      }
    ]
  },
  {
    "id": "Q19",
    "difficulty_weight": 1.0,
    "function": "infrared_app.c (includes)",
    "question": "What are all `#include` directives in `infrared_app.c`? List them in source order, exactly as they appear.",
    "rubric": [
      {
        "criterion": "First include is `\"infrared_app_i.h\"`",
        "points": 1
      },
      {
        "criterion": "Includes `<power/power_service/power.h>`, `<toolbox/path.h>`, and `<toolbox/saved_struct.h>`",
        "points": 2
      },
      {
        "criterion": "Last include is `<dolphin/dolphin.h>`; total count is exactly 6",
        "points": 1
      }
    ]
  },
  {
    "id": "Q20",
    "difficulty_weight": 1.0,
    "function": "ibutton.c (get_globals)",
    "question": "What file-scope global variables are declared in `main/ibutton/ibutton.c`? For each, give its exact name and type.",
    "rubric": [
      {
        "criterion": "`sequence_blink_set_yellow` and `sequence_blink_set_magenta` are both `const NotificationSequence` (accept with or without `static`).",
        "points": 1
      },
      {
        "criterion": "`ibutton_notification_sequences` is a `const NotificationSequence*` array (accept with or without `static`).",
        "points": 1
      },
      {
        "criterion": "No functions are listed as globals; only the 3 variable declarations are reported",
        "points": 1
      }
    ]
  },
  {
    "id": "Q21",
    "difficulty_weight": 1.5,
    "function": "ArchiveTabEnum (find_users)",
    "question": "Which functions take `ArchiveTabEnum` as a parameter type in the archive module? List all function names.",
    "rubric": [
      {
        "criterion": "Lists `archive_set_tab` (defined in archive_browser.c)",
        "points": 1
      },
      {
        "criterion": "Lists all three static inline header functions: `archive_get_tab_filetype`, `archive_get_tab_ext`, `archive_get_default_path` (these are defined only in archive_browser.h, not in any .c file)",
        "points": 2
      }
    ]
  },
  {
    "id": "Q22",
    "difficulty_weight": 1.5,
    "function": "archive_app→view_dispatcher_alloc (get_path)",
    "question": "Trace the call path from `archive_app` to `view_dispatcher_alloc`. List each function in the path in order, from start to end.",
    "rubric": [
      {
        "criterion": "Path starts with `archive_app`",
        "points": 1
      },
      {
        "criterion": "`archive_alloc` is the intermediate step",
        "points": 1
      },
      {
        "criterion": "Path ends at `view_dispatcher_alloc`",
        "points": 1
      }
    ]
  }
]
